<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ElvishTranslator" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->


    </style>

    <style name="NoActionBar" parent="Theme.Material3.DayNight.NoActionBar" >
        <item name="colorPrimary">@color/amarillo</item>
        <item name="android:statusBarColor">@color/amarillo</item>
    </style>

    <!-- Nuevo estilo con ActionBar -->
    <style name="WithActionBar" parent="Theme.Material3.DayNight">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="colorPrimary">@color/verde</item>
        <item name="android:statusBarColor">@color/verde</item>
        <!-- El tema base Theme.Material3.DayNight ya incluye ActionBar -->
    </style>
</resources>